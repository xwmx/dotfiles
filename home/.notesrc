###############################################################################
# .notesrc
#
# Configuration file for notes
#
# https://github.com/alphabetum/notes
###############################################################################

# $NOTES_AUTO_SYNC
#
# Default: 0
#
# When set to '1', operations that trigger a git commit like `add`, `edit`,
# and `delete` will also sync to the remote git repository.
export NOTES_AUTO_SYNC=1

# $NOTES_DIR
#
# Default: "$HOME/.notes"
#
# The location of the base notes directory.
# export NOTES_DIR="$HOME/.notes"

# $EDITOR
#
# Set editor to `emacsclient`, using `--alternative-editor=''` to start up
# `emacs --daemon` if it's not already running. Redirect stderr with
# `2>/dev/null` to avoid excessive output when the daemon starts.
export EDITOR="emacsclient -q --alternate-editor='' 2>/dev/null"
